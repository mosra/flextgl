# kate: indent-width 2;

matrix:
  include:
    - language: python
      python: 3.4
      env:
      - JOBID=py34
    - language: python
      python: 3.5
      env:
      - JOBID=py35
    - language: python
      python: 3.6
      env:
      - JOBID=py36

install:
  - pip install wheezy.template coverage codecov

script:
  - coverage run -m unittest

after_success:
  - codecov
